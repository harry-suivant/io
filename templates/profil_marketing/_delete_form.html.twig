<form method="post" action="{{ path('profil_marketing_delete', {'id': profil_marketing.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ profil_marketing.id) }}">
    <button class="btn">Delete</button>
</form>
