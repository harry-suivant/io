{% extends (is_granted("ROLE_ADMIN")) ? ("admin/base.html.twig") : ("customer/base.html.twig") %}

{% block title %}Etape
{% endblock %}

{% block body %}

	<section class="container col-auto col-md-9 col-xl-10">
		<div>
			<h1>Etape</h1>
			<a class="btn my-3" href="{{ path('step_index') }}">retour à la liste</a>
			<table class="table">
				<tbody>
					<tr>
						<th>Nom</th>
						<td>{{ step.name }}</td>
					</tr>
					<tr>
						<th>Actions</th>
						<td>
							{% for action in step.actions %}
								{{ action.name }}</br>
						{% endfor %}
						<td></td>
					</tr>
				</tbody>
			</table>
			<div class="row">
				<div class="col-3">
					<a class="btn my-3" href="{{ path('step_edit', {'id': step.id}) }}">modifier</a>
				</div>
				<div class="col-3">
					{{ include('step/_delete_form.html.twig') }}
				</div>
			</div>
		</div>
	</section>

{% endblock %}
