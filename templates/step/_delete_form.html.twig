<form method="post" action="{{ path('step_delete', {'id': step.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ step.id) }}">
    <button class="btn btn-orange my-3">Supprimer</button>
</form>
